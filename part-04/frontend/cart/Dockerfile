# Use a lightweight Node.js image
FROM node:18-slim

# Set the working directory
WORKDIR /app

# Copy the backend code (we'll create server.js next)
COPY server.js .

# Expose the port your app will run on
EXPOSE 3000